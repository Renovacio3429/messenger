{
  "name": "yandex-practicum-messenger",
  "version": "1.0.0",
  "source": "src/pages/index.html",
  "scripts": {
    "start": "npm run build && node server.js",
    "dev": "npm run build && parcel -p 3000 --open",
    "build": "npm run clean && parcel build src/pages/*.html",
    "clean": "rm -rf dist/*"
  },
  "license": "ISC",
  "dependencies": {
    "handlebars": "4.7.7",
    "parcel": "2.7.0",
    "postcss": "8.4.17",
    "autoprefixer": "9.8.8"
  },
  "browserslist": [
    "defaults and supports es6-module",
    "maintained node versions"
  ],
  "engines" : {
    "node" : ">=12.0.0"
  }
}